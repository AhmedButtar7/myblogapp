<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/nav_header_height"
    android:gravity="bottom"
    android:orientation="vertical"
    android:padding="@dimen/nav_header_main_padding"
    android:background="?attr/colorPrimaryDark">

    <ImageView
        android:id="@+id/navHeaderImageView"
        android:layout_width="72dp"
        android:layout_height="72dp"
        android:scaleType="centerCrop"
        android:src="@drawable/ic_profile" />

    <TextView
        android:id="@+id/navHeaderUsernameTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/UserName"
        android:textAppearance="@style/TextAppearance.AppCompat.Body1"
        android:textSize="@dimen/nav_header_main_text_size" />

    <TextView
        android:id="@+id/navHeaderEmailTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/userEmail" />
</LinearLayout>
